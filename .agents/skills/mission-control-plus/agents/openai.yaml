interface:
  display_name: "Mission Control Plus"
  short_description: "Repo-specific patterns and workflows for mission-control-plus"
  default_prompt: "Use the mission-control-plus repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true